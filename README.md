# Grow a Garden 2

Roblox SEO companion site generated with the `roblox-site-architect` workflow.

## Local commands

```bash
npm install
npm run dev
npm run build
```

The current machine used to scaffold this site did not have `node`, `npm`, or `git` installed, so build and deployment need to run in an environment with Node.js.
